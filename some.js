// comprueba si al menos un elemento del array cumple con la condición

const array = [1, 2, 3, 4];

let resultado = array.some(e => e === 4 );

// Si encuentra un elemento que cumpla la condicion, devuelve: verdadero sino es falso 

console.log(resultado);

// Devuelve: TRUE
