// ordena los elementos de un arreglo (array) y devuelve el arreglo ordenado.

const array = ['zapato', 'avion', 'camisa', 'hielo'];

let resultadp = array.sort()

console.log(JSON.stringify(resultadp))

// devuelve: ["avion","camisa","hielo","zapato"]