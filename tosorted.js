// Devuelve una versión ordenada del array sin modificar el original.

const array = [7, 2, 0, 20, 60];

const nuevoarray = array.toSorted();

console.info(nuevoarray);

// Devuelve: [ 0, 2, 20, 60, 7 ]