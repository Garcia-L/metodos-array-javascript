// Crea un nuevo array transformando cada elemento del original

const array = ["azul", "rojo", "verde"];

const nuevo = array.map(e => e + ' map');

console.log(nuevo);
// Devuelve: [ 'azul map', 'rojo map', 'verde map' ]