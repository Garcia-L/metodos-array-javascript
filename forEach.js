//Sirve para recorrer el array, 1 por 1

const array1 = ["a", "b", "c"];

// en este caso se reccorre el array y se imprime en consola el elemento o dato del array
array1.forEach((e) => console.log(e));

// Devuelve = a b c